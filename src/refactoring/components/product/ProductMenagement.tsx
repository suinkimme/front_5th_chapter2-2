import { SectionTitle, NewProductForm, ProductList } from "../";

const ProductMenagement = () => {
  return (
    <div>
      <SectionTitle>상품 관리</SectionTitle>
      <NewProductForm />
      <ProductList />
    </div>
  );
};

export default ProductMenagement;
